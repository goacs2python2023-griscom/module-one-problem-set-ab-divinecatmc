import math
def area():

    radius = float(input("Enter radius: "))

    ar = radius * radius * 3.14

    print("The area of a circle with radius "+ str(radius) + " is approximately " + str(ar) + ".")

area()